namespace PrizeDraw.Interfaces
{   //Provides the interface for an instance that will calculate the total sum of prize money
    public interface IPrizeMoneyCalculator
    {
        int CalculateTotal();
    }
}
